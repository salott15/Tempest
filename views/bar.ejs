<!DOCTYPE html>
<html>
<head>
	<title>Bar</title>

	<link rel="stylesheet" type="text/css" href="/main.css">

  <link href="https://fonts.googleapis.com/css?family=Abril+Fatface|Open+Sans+Condensed:300" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css?family=Sanchez" rel="stylesheet">

  <script
    src="https://code.jquery.com/jquery-3.2.1.min.js"
    integrity="sha256-hwg4gsxgFZhOsEEamdOYGBf13FyQuiTwlAQgxVSNgt4"
    crossorigin="anonymous"></script>

    <script src="/app.js"></script>
 
</head>
<body>

<% include nav.ejs %>


 <h2><%= bar.barname %></h2>

 <h3>This bar is currently <%= bar.busy %>.<br><% if(bar.busy != "not rated") { %> Disagree?<% } %> Vote full or empty by clicking the icons below.</h3>

<br><br>

 
<div class = "voteFull" 
data-barname = "<%= bar.barname %>" 
data-barId = "<%= bar.barname %><%= bar.lat %><%= bar.lng %>" 
data-lat = "<%= bar.lat %>"
data-lng = "<%= bar.lng %>">
 <img src="/fullbeer.png">
 </div>

 <div class = "voteEmpty" data-barname = "<%= bar.barname %>" 
data-barId = "<%= bar.barname %><%= bar.lat %><%= bar.lng %>" 
data-lat = "<%= bar.lat %>"
data-lng = "<%= bar.lng %>">
 <img src="/emptybeer.png">
 </div>

 <br><br><br>

<% if(bar.busy != "not rated" && user) { %>
<div class = "saveBar" data-barname = "<%= bar.barname %>" 
data-barid = "<%= bar.barname %><%= bar.lat %><%= bar.lng %>" 
data-lat = "<%= bar.lat %>"
data-lng = "<%= bar.lng %>">
	Save bar
</div><% } %>
</body>
</html>